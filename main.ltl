// ltl-bignum/main.ltl
//
// Arbitrary-precision integer arithmetic (+, *, /, mod).
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-bignum v0.1.0")
    io::println("Arbitrary-precision integer arithmetic (+, *, /, mod).")
    0
}
